{
  "name": "challenge_3",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "dependencies": {
    "bootstrap": "^4.5.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.4",
    "react": "^0.14.7",
    "react-dom": "^0.14.7"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-plugin-react": "1.0.0",
    "babel-preset-react": "^6.5.0"
  },
  "scripts": {
    "watch": "babel . --watch --out-dir compiled --presets=react --ignore=node_modules,compiled --source-maps inline",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js",
    "build": "babel . --out-dir compiled --presets=react --ignore=node_modules,compiled,server.js,stylesheet.css --source-maps inline"
  },
  "author": "",
  "license": "ISC"
}
